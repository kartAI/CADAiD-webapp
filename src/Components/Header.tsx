import { Box } from "@mui/material";

const headercontent = "Last opp vedlegg";

export default function Header() {
    return(
        <Box component="section"><h1>{headercontent}</h1></Box>
    );
}